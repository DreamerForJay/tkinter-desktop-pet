# 期末專題簡報生成 Prompt（Claude Code 專用）

> **使用方式**：將本檔案內容貼入 Claude.ai 或 Claude Code，即可生成完整 20 分鐘期末報告簡報內容。

---

## 【指令開始】

你現在同時扮演：
- 資訊工程系教授（學術嚴謹角度）
- 專題評審委員（從功能完整性評分）
- 資深軟體工程師（技術深度分析）
- 技術講師（清楚解說架構）
- 產品經理（使用者體驗視角）

---

## 專案背景

本專題為「大學資訊工程系 Python Tkinter 期末專題」。

**專題名稱：** 虛擬桌面寵物（Desktop Pet）— 桌面陪伴生產力工具

**課程要求：**
- 使用 Tkinter 製作 GUI 應用程式
- 製作簡報並上台報告 20 分鐘
- 報告內容需包含：設計理念、使用的函式庫、遇到的困難、解決方式、結論、作品展示
- 書面報告：至少 6 頁、至少 1000 字（不含程式碼）

---

## 完整功能清單（依實作完成順序）

### 🔧 核心系統
1. **MVC 架構**（Model / Service / View / Controller 四層，單一 `desktop_pet.py` 約 3800 行）
2. **`_AutoSaver` 非同步存檔**：daemon 執行緒每次狀態變更自動寫入 `data.json`，重啟後完整復原
3. **`AnimationCache` PNG 序列快取**：自然排序、多角色路徑解析、缺失狀態 fallback 至 idle
4. **`PomodoroTimer` 狀態機**：`root.after(1000)` 驅動，工作→短休息→大休息週期，`update_config()` 熱更新
5. **`MusicPlayer` 漸入漸出**：pygame 背景執行緒平滑調整音量（2 秒淡化）

### 🎨 視覺介面
6. **無邊框透明視窗**：`overrideredirect=True` + `-transparentcolor` 實現 PNG 透明背景
7. **`TimerBubble` 計時 HUD**：Canvas 繪製圓角計時器，三色進度條（紅/綠/藍）
8. **`SpeechBubble` 對話氣泡**：獨立 Toplevel 浮動視窗，跟隨拖曳移動，N 秒自動隱藏
9. **`_PopupMenu` 自訂右鍵選單**：Toplevel 模擬，支援子選單、Hover 觸發、全域點擊關閉

### 🐾 角色系統
10. **多角色支援**：帥潮教授（預設）+ 小紫（預設）+ 5 種需解鎖抽蛋角色
11. **`EggGachaScreen` 孵蛋動畫**：7 次點擊破蛋，粒子爆炸效果，揭曉時顯示真實角色 sprite
12. **`FarewellScreen` 放生動畫**：日落天空場景，角色真實 sprite 漸遠縮小，打字機台詞，略過按鈕
13. **匯入自訂角色**：右鍵選單 → 選取含 `idle/` 子目錄的資料夾，自動複製並加入清單
14. **`characters.json` 角色名稱映射**：外部設定檔管理顯示名稱

### 🪙 經濟 & 商店
15. **金幣系統**：番茄鐘 +10、簽到 +5、禮盒隨機、加倍符文 ×2
16. **`ShopView` 商店視窗**：食物（6 種）+ 道具（4 種）+ 角色蛋
17. **`BackpackView` 背包視窗**：購買後存入，點擊使用即生效

### 📋 待辦清單（Todoist 風格）
18. **`TodoView` 主視窗**：
    - 左側優先度色條（紅/橙/綠）
    - 按到期日期自動分組：逾期→今天→明天→本週→之後→無截止
    - 相對日期顯示：「逾期 3 天」「今天 22:00」「明天」
    - 篩選 Tab：全部 / 未完成 / 今天 / 已完成
    - 底部快速新增（Enter 直送）
    - 清除已完成功能
    - 響應式視窗（可自由拉伸）
19. **`TodoEditDialog` 編輯對話框**：
    - 任務名稱、優先度（Toggle Button）、分類（下拉）
    - 到期日期時間（Spinbox 選擇器）
    - 個別提醒：到期前 N 分鐘
    - 備註多行輸入
20. **到期提醒音效**：`winsound.Beep()` 播放 C-E-G 三音提示，背景執行緒不阻塞 GUI
21. **今日待辦輪播**：每 5 分鐘在氣泡中依序提醒今日任務（含逾期）

### 🎵 音樂管理
22. **`MusicView` 音樂管理視窗**：顯示播放清單、單曲選播、刪除（含磁碟移除）、匯入

### 📊 統計（Forest App 風格）
23. **累積專注時間**：每顆番茄 +work_min 分鐘，格式化為 Xh Ym
24. **今日番茄計數**：跨日自動重置
25. **連續天數（streak）**：每天至少一顆番茄計入，中斷歸零
26. **emoji 森林視覺化**：番茄數轉換為 🌱→🌿→🌳→🌲 成長樹木

---

## 技術難點與解決方案

### 難點 1：右鍵選單在 Windows 立即消失
**問題**：`tk_popup()` 在 Windows 為非阻塞，`try/finally: unpost()` 導致瞬間關閉。
**解法**：完整捨棄 `tk.Menu`，改用 `_PopupMenu`（Toplevel + Frame 手刻）模擬選單行為，支援子選單 Hover 觸發、全域點擊關閉。

### 難點 2：多角色 PNG 路徑解析 & 首字母自然排序
**問題**：`slice_10.png` 在字母排序下排在 `slice_2.png` 之前，動畫幀序錯亂。
**解法**：實作 `_nat_key()` 自然排序函式（`re.split(r'(\d+)', s)`），缺失狀態 fallback 至 idle 動畫。

### 難點 3：設定視窗 Combobox 無法點開
**問題**：`grab_set()` 獨佔事件，阻止 ttk.Combobox 的下拉 Toplevel 接收滑鼠事件。
**解法**：移除 `grab_set()`，改用 ttk.Combobox 本身的焦點管理。

### 難點 4：TodoEditDialog 儲存失效
**問題**：`_save()` 傳遞 `due_datetime=` / `remind_minutes=` 給 `add_todo()`，但方法簽名為 `due=` / `remind=`，TypeError 被靜默吞掉。
**解法**：分開新增與編輯路徑，新增使用正確參數名，編輯透過 `update_todo(**fields)` 直接更新 dict。

### 難點 5：pack 與 grid 混用導致 TodoView 無法響應式延伸
**問題**：tkinter 不允許同一個容器中混用 pack 和 grid。
**解法**：將整個 TodoView 視窗改為純 grid 佈局，設定 `rowconfigure(2, weight=1)` 讓清單區垂直延伸。

---

## 使用的函式庫

| 函式庫 | 版本 | 用途 |
|-------|------|------|
| tkinter | 內建 | GUI 主框架（Toplevel, Canvas, ttk） |
| Pillow (PIL) | 10.x | PNG 圖片載入、RGBA 合成、縮放 |
| pygame | 2.6 | 背景音樂播放、淡入淡出音量控制 |
| winsound | 內建 (Windows) | 待辦到期提示音 |
| threading | 內建 | `_AutoSaver` daemon 執行緒、音樂淡化執行緒 |
| json | 內建 | 存檔序列化 |
| uuid | 內建 | 待辦清單唯一 ID 生成 |
| shutil | 內建 | 角色素材 / 音樂檔案複製 |
| math, random, re | 內建 | 動畫計算、隨機事件、自然排序 |
| PyInstaller | 6.x | 打包為 Windows exe |

---

## 簡報結構（共 16~20 張投影片，20 分鐘）

### 第一章：動機與設計理念（3 分鐘，3 張）

**投影片 1 — 封面**
- 標題：虛擬桌面寵物 Desktop Pet
- 副標：桌面陪伴生產力工具 | Python Tkinter 期末專題
- 成員名稱 / 學號 / 日期

**投影片 2 — 問題背景**
- 學生在讀書時容易分心、缺乏動力
- 現有解法：Forest App（手機，不能開電腦）、番茄鐘（功能單一）
- 我們的解法：把番茄鐘 + 養成遊戲 + 待辦清單整合到桌面寵物

**投影片 3 — 設計理念**
- 核心理念：陪伴感 + 正向回饋迴圈
- 使用者流程：開始番茄 → 賺金幣 → 養寵物 → 達成待辦 → 更有動力繼續

---

### 第二章：功能展示（10 分鐘，8 張）

**投影片 4 — 角色系統**
- 帥潮教授 / 小紫（預設）
- 5 種抽蛋角色（商店 30 金幣）
- 孵蛋互動截圖（7 次點擊破蛋動畫）
- 放生動畫截圖（日落場景 + sprite 漸遠）

**投影片 5 — 番茄鐘 & 心情系統**
- TimerBubble HUD 截圖
- 三段式計時：工作→短休息→大休息
- 心情衰減機制（每 90 秒 -1%）
- 對話氣泡範例

**投影片 6 — 商店 & 背包**
- ShopView 截圖
- 食物 / 道具 / 角色蛋三大類
- 購買流程演示

**投影片 7 — 待辦清單**
- TodoView 截圖（分組顯示）
- TodoEditDialog 截圖（日期時間設定）
- 強調：Todoist 風格設計、相對日期、到期提醒

**投影片 8 — 統計 & Forest 視覺**
- StatsView 截圖
- 累積專注時間 / 今日番茄 / 連續天數
- emoji 森林 🌱→🌲 成長截圖

**投影片 9 — 音樂管理**
- MusicView 截圖
- 選曲 / 刪除 / 匯入功能

**投影片 10 — 右鍵選單**
- _PopupMenu 截圖（含完整選項）
- 強調：自訂 Toplevel 模擬，解決 Windows 立即消失 bug

**投影片 11 — 匯入自訂角色**
- 操作示範截圖（右鍵 → 匯入角色素材）
- 資料夾結構說明

---

### 第三章：技術架構（5 分鐘，4 張）

**投影片 12 — MVC 架構圖**
- Mermaid flowchart：Model / Service / View / Controller
- 強調：單一檔案 3800 行，四層明確分離

**投影片 13 — 技術難點 1 & 2**
- 右鍵選單 Windows bug → _PopupMenu 解法
- PNG 自然排序 → `_nat_key()` 解法

**投影片 14 — 技術難點 3 & 4**
- grab_set() 阻塞 Combobox → 移除解法
- TodoEditDialog 儲存失效 → 參數名稱修正

**投影片 15 — 核心技術摘要**
- 使用函式庫列表（表格）
- 特別強調：threading daemon、pygame 背景執行緒、PIL RGBA 合成

---

### 第四章：結論 & 展望（2 分鐘，2 張）

**投影片 16 — 成果總結**
- 完成功能數量（26 項）
- 程式碼規模：單檔 ~3800 行
- 學習收穫：MVC 設計模式、threading、GUI 事件系統

**投影片 17 — 未來展望**
- 跨平台支援（macOS / Linux）
- 寵物互動（點頭 / 揮手等動作回饋）
- 雲端同步存檔
- 更多角色 DLC / 皮膚系統
- 行動版（Android）

---

## 生成要求

請依照上述結構生成完整 17 張投影片的文字內容，要求：

1. **每張投影片**提供：標題、3~5 個重點 bullet（每點 1~2 行）、演講稿提示（口說重點，括號標示）
2. **技術難點**要有「問題情境 → 嘗試方法 → 最終解法」三段式敘述
3. **語氣**：正式但不死板，適合 20 分鐘口頭報告
4. **時間分配**：每張投影片標示預估說明時間
5. **視覺建議**：每張提供配色建議（主色 / 強調色）與版面提示

---

## 書面報告結構（至少 6 頁 1000 字）

請額外生成書面報告大綱：

1. **摘要**（200 字）
2. **設計理念與動機**（300 字）
3. **系統功能介紹**（400 字 + 截圖說明位置）
4. **技術架構說明**（400 字 + MVC 架構圖說明）
5. **遇到的困難與解決方式**（400 字，列舉至少 4 個難點）
6. **結論與心得**（300 字）
7. **參考資料**

---

## 【指令結束】

請開始生成完整簡報內容，格式清楚分章節投影片，並在每張投影片後附上演講稿提示。
