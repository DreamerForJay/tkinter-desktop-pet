# 虛擬桌面寵物（Desktop Pet）期末專題報告

---

## 一、研究動機

現代學生與上班族長時間使用電腦學習與工作，容易產生疲勞、缺乏陪伴感與時間管理問題。

本專題希望開發一款桌面虛擬寵物系統，讓使用者在工作與讀書過程中，透過可愛角色互動、番茄鐘提醒與陪伴機制，提升專注力與使用體驗。除桌面裝飾功能外，也整合生產力工具與遊戲化元素，讓使用者更願意持續使用。

---

## 二、參考作品與差異分析

### 參考作品：VPet（Steam）

VPet 是一款功能完整的商業桌面寵物，支援大量互動、多角色與養成系統，但整體設計偏向成熟的商業產品，功能龐大，使用門檻較高。

### 本專題特色

| 面向 | 本專題 |
|------|--------|
| 角色素材 | 自行設計並使用 AI 輔助生成 |
| 核心定位 | 桌面陪伴 ＋ 生產力工具 |
| 特色功能 | 番茄鐘計時 ＋ 金幣獎勵機制 |
| 設計方向 | 輕量化、操作簡單 |

---

## 三、系統功能

| 功能模組 | 說明 |
|----------|------|
| 虛擬角色顯示 | 多狀態 PNG 動畫：待機、寫程式、讀書、吃東西、拖曳、睡覺 |
| 番茄鐘計時器 | 可調整工作／短休息／長休息時長，完成後自動獎勵金幣 |
| 金幣與心情系統 | 番茄鐘、每日簽到累積金幣；心情每 90 秒自動衰減 |
| 虛擬商店 | 食物補充心情；道具（藥水、禮盒、加倍符文、蝴蝶結）各有效果 |
| 背包系統 | 購買後存入背包，隨時使用 |
| 對話氣泡 | 工作關心、閒聊、吃東西、心情低落時主動顯示對話 |
| 個人化設定 | 寵物名稱、番茄鐘時長、永遠置頂 |
| 自動存檔 | 所有資料儲存至 `data.json`，重啟後完整保留 |
| 讀書音樂 | 番茄鐘工作階段自動播放背景音樂，支援漸入漸出 |

---

## 四、系統架構

本程式採用 **MVC（Model-View-Controller）** 分層架構，所有程式碼集中於 `desktop_pet.py`。

```
PetModel          ← 資料層：金幣、心情、背包、設定、統計
  └── _AutoSaver  ← 背景執行緒非同步寫入 JSON

PomodoroTimer     ← 番茄鐘狀態機（root.after 驅動）
MusicPlayer       ← pygame 背景音樂（漸入漸出）
AnimationCache    ← PNG 序列快取
SpeechBubble      ← 浮動對話氣泡（獨立 Toplevel）

PetView           ← 視圖層：主視窗、動畫、子視窗工廠
  ├── TimerBubble ← 番茄鐘計時框（嵌入主視窗）
  ├── ShopView    ← 商店視窗
  ├── BackpackView← 背包視窗
  ├── StatsView   ← 統計視窗
  └── SettingsView← 設定視窗

PetController     ← 控制層：事件處理、業務邏輯、對話排程
```

**資料流：** View 事件 → Controller → Model setter（自動觸發非同步存檔）→ Controller 更新 View

---

## 五、開發過程中遇到的問題與解決方式

### 問題一：右鍵選單一閃即逝

**問題原因：**  
`show_menu()` 在 `menu.tk_popup()` 後的 `finally` 區塊立即呼叫 `menu.unpost()`。在 Windows 上 `tk_popup()` 為非阻塞呼叫，`finally` 會在使用者互動前立即執行，導致選單彈出後馬上被關閉。

**解決方式：**  
移除 `finally` 中的 `menu.unpost()`，改用 `<Unmap>` 事件綁定進行事後清理，只保留 `grab_release()` 確保焦點正確釋放。

---

### 問題二：使用食物時心情仍持續衰減

**問題原因：**  
`on_hp_tick()` 判斷 Controller 的 `self._status` 是否為 `"eating"`，但 `use_item()` 只呼叫了 View 的 `trigger_eating()`，只更新了 View 層的狀態，Controller 的 `_status` 從未同步變為 `"eating"`，保護邏輯形同虛設。

**解決方式：**  
使用食物時同步設定 Controller 的 `self._status = "eating"`，並在吃東西動畫結束（EATING_MS）後透過排程自動恢復原狀態。

---

### 問題三：設定頁套用後角色被重設

**問題原因：**  
`SettingsView._apply()` 將 `character` 硬寫為 `"default"` 傳入 `apply_settings()`，每次套用設定都會覆蓋使用者儲存的角色選擇。

**解決方式：**  
改為讀取 `model.settings.get("character", "default")`，套用設定時保留目前已儲存的角色設定。

---

### 問題四：關閉程式時對話氣泡可能拋出 TclError

**問題原因：**  
對話氣泡的 `after()` callback 可能在主視窗已銷毀後才執行，導致 `_ensure_win()` 嘗試對已銷毀的 root 建立新 `Toplevel`，拋出未處理的 `TclError`。

**解決方式：**  
`_ensure_win()` 加入 `winfo_exists()` 檢查；`show()` 加入 try/except 確保安全退出；`_quit()` 加入取消所有 after 排程的流程。

---

### 問題五：素材路徑不一致

**問題原因：**  
gitignore 排除 `assets/` 資料夾後，重新加入素材時，實際路徑變成 `assets/assets/...`（雙層），程式原本預期 `assets/...`（單層）。

**解決方式：**  
程式中相容兩種路徑結構，能自動找到正確的素材位置。

---

### 問題六：圖片黑色背景破圖

**問題原因：**  
透明背景 PNG 在合成到 Tkinter Label 時若未正確處理 alpha 通道，會顯示黑色背景。

**解決方式：**  
`AnimationCache` 載入圖片時，以 `Image.paste(img, mask=img.split()[3])` 將透明度合成到白色背景，確保去背正確顯示。

---

## 六、技術實作重點

**視窗透明化**

```python
root.overrideredirect(True)          # 移除標題列與邊框
root.attributes("-transparentcolor", "white")  # 白色視為透明
```

**動畫驅動**

透過 `root.after(200, self._animate)` 迴圈每 200ms 切換一幀 PNG，根據目前 `_status` 決定播放哪組動畫序列。

**番茄鐘狀態機**

`PomodoroTimer` 使用 `root.after(1000, self._tick)` 驅動秒數倒數，狀態在 `work → rest → work` 或 `work → long_rest → work` 間自動切換，並透過 callback 通知 Controller 做後續處理（獎勵金幣、切換音樂、顯示通知）。

**非同步存檔**

`_AutoSaver` 使用 daemon 背景執行緒寫入 JSON，避免 GUI 卡頓。程式正常關閉時呼叫 `sync_save()` 做最後一次同步存檔，確保資料不遺失。

---

## 七、成果展示

- 寵物待機動畫（右下角顯示）
- 右鍵選單（活動切換、商店、背包、番茄鐘）
- 番茄鐘倒數與計時氣泡
- 商店購物介面
- 吃東西動畫
- 對話氣泡提醒
- 設定視窗

---

## 八、未來展望

| 功能 | 說明 |
|------|------|
| 角色切換 UI | 設定頁加入角色選擇下拉選單 |
| AI 對話功能 | 整合 ChatGPT / 本地 LLM，讓角色與使用者互動 |
| 白噪音系統 | 雨聲、咖啡廳音效等專注輔助音效 |
| 寵物蛋系統 | 孵化與蒐集多種角色 |
| 雲端同步 | 存檔上傳，跨裝置使用 |
| 成就系統 | 累積番茄鐘、連續簽到等成就獎勵 |

---

## 工作分配

| 成員 | 負責項目 |
|------|----------|
| 黃耀賢 | 功能檢查、角色與設定相關問題修正 |

---

## 老師可能提問與回答方向

**Q：你們和 VPet 差在哪裡？**  
我們定位為輕量化的學習陪伴工具，加入番茄鐘計時與金幣獎勵機制，角色素材為自製，整體比 VPet 更專注在生產力輔助場景。

**Q：技術上怎麼實作？**  
使用 Python Tkinter 製作 GUI，透過 `overrideredirect` 去除視窗邊框並以透明色實現去背效果，動畫以 PNG 序列配合 `root.after` 迴圈驅動，番茄鐘為狀態機設計，資料以 JSON 持久化儲存。

**Q：未來可以怎麼改進？**  
可加入 AI 對話功能、多角色系統、白噪音、成就系統，讓產品更完整且具持續遊玩性。
